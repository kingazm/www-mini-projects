# Juwenalia Krakowskie Info App
This is a react app displaying info about student event in Cracow, Juwenalia. It features reading data from JSON file and displaying via props, functional components, and elegant responsive design.

## To run the app locally:
```bash
npm install
npm start
```

The app will be visible on localhost:3000
